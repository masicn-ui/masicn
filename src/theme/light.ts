// theme/light.ts — default light theme.
// The active palette is defined in palettes/masi.ts.
// CLI overwrites this file when a non-masi palette is selected during `masicn init`.
export { lightTheme } from './palettes/masi';
