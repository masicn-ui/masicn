/**
 * Bump when a new semantic color token is added to the Theme interface.
 * Custom palettes that declare an older themeVersion get a compatibility
 * warning from masicn doctor.
 */
export const THEME_VERSION = '1.0';
